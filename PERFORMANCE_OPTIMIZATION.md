# Оптимизация производительности Mobile

## ✅ Уже сделано

### 1. **Отложенная загрузка скриптов (defer)**
- Все JavaScript файлы загружаются с атрибутом `defer`
- Принцип: браузер начинает их загружать, но не блокирует парсинг HTML
- Результат: страница отображается быстрее на мобильных устройствах

### 2. **Ленивая загрузка Chart.js**
- Chart.js загружается только когда пользователь переходит на страницу с графиками
- Экономит ~100KB на первоначальной загрузке
- Функция `window.getChartJS()` загружает библиотеку при первом обращении

### 3. **Оптимизация загрузки Font Awesome**
- Используется `rel="preload"` с асинхронной загрузкой
- Font Awesome загружается не блокируя отображение страницы
- Fallback через `<noscript>` для браузеров без JS

### 4. **Улучшенный Service Worker**
- Стратегия кэширования оптимизирована для мобилей
- Локальные файлы: сначала кэш, потом сеть (быстрый офлайн)
- CDN ресурсы: сначала сеть, потом кэш (актуальные версии)
- Автоматическое очищение старого кэша

### 5. **Отложенная загрузка Firebase**
- Firebase библиотеки загружаются с `defer`
- Не блокируют первоначальное отображение страницы

---

## 📊 Ожидаемые улучшения производительности

| Метрика | До | После | Улучшение |
|---------|----|----- |-----------|
| First Contentful Paint (FCP) | ~1.5s | ~0.8s | -47% ⚡ |
| Largest Contentful Paint (LCP) | ~2.5s | ~1.3s | -48% ⚡ |
| Cumulative Layout Shift (CLS) | ~0.15 | ~0.08 | -47% |
| Total Bundle Size | ~250KB | ~150KB | -40% |

---

## 🚀 Дополнительные рекомендации для дальнейшей оптимизации

### 1. **Minify CSS и JS**
Сжатие файлов с помощью инструментов:
```bash
# Для CSS
npm install -g csso-cli
csso ./public/css/modern-style.css -o ./public/css/modern-style.min.css

# Для JS (если нужен)
npm install -g terser
terser ./public/js/ui.js -o ./public/js/ui.min.js
```

### 2. **Критический CSS**
Выделите критический CSS для первоначального отображения и вставьте в `<head>`:
```html
<style>
  /* Критические стили для hero секции */
  .hero, .header { /* inline styles */ }
</style>
```

### 3. **Image Optimization**
- Используйте WebP с fallback PNG
- Установите атрибуты `width` и `height` для изображений
- Используйте `srcset` для разных размеров экранов

### 4. **Lazy Loading изображений**
```html
<img src="image.jpg" loading="lazy" alt="Description">
```

### 5. **Content Security Policy (CSP)**
Добавьте в Netlify функцию для установки заголовков:
```javascript
// netlify/functions/security.js
exports.handler = async () => {
  return {
    headers: {
      'Cache-Control': 'public, max-age=31536000',
      'X-Content-Type-Options': 'nosniff',
    }
  };
};
```

### 6. **Кэширование на длительный период**
В `netlify.toml`:
```toml
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
```

### 7. **Используйте WebP для изображений**
Браузеры поддерживают более эффективное сжатие:
```html
<picture>
  <source srcset="image.webp" type="image/webp">
  <img src="image.png" alt="Description">
</picture>
```

### 8. **Динамический import для тяжёлых модулей**
```javascript
// Вместо:
import { heavyModule } from './heavy.js';

// Используйте:
const heavyModule = await import('./heavy.js');
```

### 9. **Gzip/Brotli сжатие**
Убедитесь, что Netlify сжимает ответы (обычно включено по умолчанию).

### 10. **Мониторинг производительности**
Добавьте Web Vitals мониторинг:
```javascript
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

getCLS(metric => console.log('CLS:', metric.value));
getLCP(metric => console.log('LCP:', metric.value));
```

---

## 🛠️ Тестирование производительности

### Online Tools:
- **Google PageSpeed Insights**: https://pagespeed.web.dev/
- **WebPageTest**: https://www.webpagetest.org/
- **GTmetrix**: https://gtmetrix.com/

### Chrome DevTools:
1. Откройте DevTools (F12)
2. Вкладка **Network** - проверьте размер и время загрузки файлов
3. Вкладка **Performance** - профилируйте загрузку страницы
4. Вкладка **Lighthouse** - запустите аудит производительности

### Симуляция мобильного соединения:
```
DevTools > Network > Throttling > Slow 3G
```

---

## 📱 Особенности мобильной оптимизации

### Already Implemented:
✅ Viewport meta tag
✅ Responsive CSS (media queries)
✅ Touch-friendly buttons (min 44px)
✅ Service Worker для offline
✅ PWA manifest

### To Consider:
- Viewport-relative font sizes
- Touch-optimized UI (larger padding/margins)
- Fewer network requests on slow connections
- Progressive enhancement

---

## 📈 Метрики для мониторинга

Проверьте эти метрики через Google Analytics или Lighthouse:

- **Core Web Vitals**:
  - LCP (Largest Contentful Paint) < 2.5s ✅
  - FID (First Input Delay) < 100ms
  - CLS (Cumulative Layout Shift) < 0.1

- **Performance Budget**:
  - Total JS < 150KB
  - Total CSS < 50KB
  - Первоначальная загрузка < 1s на 3G

---

## 🔄 Обновление при изменении файлов

При обновлении файлов обновляйте версию кэша в `service-worker.js`:
```javascript
const CACHE_NAME = 'money-in-sight-v3'; // Увеличьте номер
```

Старые версии кэша будут автоматически удалены.

---

Документация создана: 6 февраля 2026 года
