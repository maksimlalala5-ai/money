{
  "name": "money-in-sight",
  "version": "1.0.0",
  "description": "Умный контроль финансов",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build completed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}